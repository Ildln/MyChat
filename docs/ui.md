# UI

## Новая структура интерфейса

Текущий UI в `app/static/index.html` теперь разделён на два основных сценария:

1. экран авторизации
2. основной экран приложения после входа

Интерфейс стал ближе к обычному мессенджеру: слева чаты, по центру область диалога, справа друзья и заявки.

## Экран авторизации

Если токена нет в `localStorage`, пользователь видит только auth screen.

Что есть на этом экране:

- отдельная вкладка входа
- отдельная вкладка регистрации
- сообщения об ошибках и успешных действиях
- автоматический переход в основной экран после успешного входа или регистрации

## Основной экран приложения

Если токен есть в `localStorage`, пользователь сразу попадает в основной экран приложения.

Структура экрана:

- слева колонка чатов
- по центру основная область диалога
- справа колонка друзей, заявок и действий

Дополнительно в шапке есть:

- информация о текущем пользователе
- кнопка обновления профиля
- logout, который очищает токен и возвращает на auth screen

## Левая колонка

Левая колонка отвечает за чаты.

Сейчас она умеет:

- создавать `direct chat` по `POST /chats/direct`
- загружать список чатов через `GET /chats`
- выбирать чат для отображения в центральной колонке

## Центральная колонка

Центральная колонка пока подготовлена под следующий этап сообщений.

Сейчас в ней отображается:

- заголовок выбранного чата
- информация об участниках
- placeholder вместо реальной переписки

Это сделано специально: структура диалога уже есть, но сама отправка и показ сообщений будут следующим этапом.

## Правая колонка

Правая колонка отвечает за друзей и заявки.

Сейчас она умеет:

- отправлять заявку в друзья
- загружать входящие заявки
- загружать исходящие заявки
- принимать входящие заявки
- отклонять входящие заявки
- загружать список друзей
- показывать состояние токена, текущего пользователя и последнего запроса

## Ограничения текущего этапа

На этом этапе UI всё ещё остаётся простым и ориентированным на ручную проверку backend.

Ограничения:

- нет полноценной переписки в центре диалога
- нет групповых чатов
- нет маршрутизации между страницами
- нет отдельного экрана настроек
- данные обновляются по кнопкам и при входе, без сложной синхронизации
